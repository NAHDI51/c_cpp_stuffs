#include <bits/stdc++.h>
using namespace std;

int dp[102][1002];
int ways(vector<int> coins, int n) {
    if(n == )
}

int main() {
    vector<int> a;
    int n, m;
    cin >> m >> n;
    while(n--) {
        int m;
        cin >> m;
        a.push_back(m);
    }
    memset(dp, -1, sizeof(dp));
    cout << ways(a, m) << '\n';
}